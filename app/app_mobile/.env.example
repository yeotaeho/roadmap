# 앱이 읽는 값은 Monorepo 루트( `docker-compose.yaml` 옆)의 `.env` 입니다.
# 루트 `.env`에 아래 키를 추가하세요. (Next.js / Docker와 동일한 파일)
#
#   API_BASE_URL=https://your-api.example.com
#
# 우선순위: `--dart-define=API_BASE_URL=...` > 루트 `.env`의 API_BASE_URL > 기본 placeholder

API_BASE_URL=https://api.example.com
