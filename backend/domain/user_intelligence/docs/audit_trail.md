# user_intelligence 작업 기록

## 2026-07-03 — SP-4: 자기모델 RIASEC 6축 점수화 + 상담실 시각화
- **무엇** — RIASEC 추출을 top_codes만 뽑던 것을 **6축 0~100 점수**로 바꾸고(대화가 쌓일수록 confidence 가중 블렌딩 + shrinkage로 안정화), `/consult` 우측 목업 지갑을 실데이터 `SelfModelPanel`(RIASEC 레이더·주요유형 배지·서사·근거칩·Big Five placeholder)로 교체. `user_self_model.riasec` JSONB 형태 `{scores,raw,weights,top_codes}`(DB 마이그레이션 없음). 순수 `blend_riasec`(축별 confidence 가중 증분 평균 `raw'=(raw·W+s·c)/(W+c)`, shrinkage `50+(raw-50)·min(1,W/K)` K=4, top_codes=display>55 상위2), 파서 6축 채점(누락 50·0), 프롬프트가 행동근거 가중·근거 얇으면 50 보수·top_codes 별도추정 금지 명시. merge에 user_form riasec 보존 가드. SP-3 임베딩·설명은 `riasec.top_codes` 계속 읽어 무영향.
- **왜** — 사용자 의도(상담실 우측에 성향 시각화)를 실데이터로. top_codes 임시 레이더 대신 실제 검사 채점 방식을 조사([research](../../../docs/research/2026-07-03-riasec-scoring-research.md): 워크넷·O*NET은 6축 독립 합산·소표본 shrinkage)해 per-axis 점수의 정당성을 확보. Big Five 추출은 동일 방법론으로 후속.
- **어디** — [riasec_scoring.py](../hub/services/riasec_scoring.py)(신규 `blend_riasec`)·[self_model_service.py](../hub/services/self_model_service.py)(merge riasec 분기)·[self_model_extraction_service.py](../hub/services/self_model_extraction_service.py)(incoming 매핑)·[core/llm/client.py](../../../core/llm/client.py)(프롬프트·파서). 프론트 `www.yeotaeho.kr/src/lib/api/selfModel.ts`·`components/features/consult/SelfModelPanel.tsx`·`ConsultView.tsx`. 스펙 [design](../../../docs/superpowers/specs/2026-07-03-self-model-riasec-scoring-viz-design.md)·플랜 [plan](../../../docs/superpowers/plans/2026-07-03-self-model-riasec-scoring-viz.md).
- **검증** — SDD 2태스크 각 리뷰 Approved(각 1 fix 라운드 — T1 user_form 보존 가드, T2 미로그인 빈상태·지갑 dead UI 제거). whole-branch(opus): Critical 0, Important 2(임베딩 자격 회귀·프론트 옛행 크래시). **Codex 3라운드**: P1 쿼리키 사용자 누출·P2 옛행 크래시·P2 신호소실 정리경로 무력화 수정 → 3라운드는 "대화 직후 패널 stale" 지적을 **기각**(자기모델=일별 배치라 대화 직후 재추출 없음·스펙상 실시간 out, 패널 캡션 고지) → 수렴. 백엔드 9스위트(riasec11·parse11·extraction14·merge12·candidacy22·reembed7·chance6·recommend6·consult13) green + tsc 0. 커밋 2eb8e40..c5e15f4.
- **후속** — **Big Five 추출**(사용자 요청 후속 논의) — blend+shrinkage 동일 방법론을 5요인에. 실시간 재추출(현재 일별)·레이더 규준집단 백분위·신뢰도 캡션 실데이터화(axisConfidence 미사용)·레이더 스크린리더 폴백. merge `merged_conf['riasec']` 중복 계산 정리.

## 2026-07-03 — 상담실 이전: 코치 대화 엔진을 user_intelligence 로 이전·전수 개명
- **무엇** — `/consult`(AI 상담실)를 성격·성향·자기이해 발견의 실엔진으로 세움. `ai_coach`에 있던 대화·세션·자기모델 추출 엔진을 **`user_intelligence` 도메인으로 이전**하고 coach→consult **전수 개명**(테이블 `coach_sessions`→`consult_sessions`·`coach_messages`→`consult_messages`, 인덱스·FK 4개, `user_self_model_evidence.coach_session_ref`→`consult_session_ref`, `source` 값·컬럼 DEFAULT `coach_extraction`→`consult_extraction`, 마이그 `59a2f51cc892` Neon 적용). 상담 맥락에서 **로드맵/퀘스트 제거**(코치 위임)하고 `_CONSULT_SYSTEM_PROMPT`(상담사 역할) 신설. API `/api/coach/*`→`/api/consult/*`. 프론트 `CoachView`→`ConsultView`·`coach.ts`→`consult.ts`, `/consult` 목업 교체·`/coach` 로드맵 코치 플레이스홀더·`/chat` 리다이렉트 `/consult`로. 대화 엔진이 자기모델 데이터층(SP-1)과 한 도메인에 응집됐다.
- **왜** — 사용자 의도는 `/consult` 탭에서의 성격 분석·성향 파악인데, 실엔진(SSE·세션·추출)이 `ai_coach`에 얹혀 `/coach`에 노출되고 `/consult`는 목업이었다. 상담실=자기이해, 코치=로드맵으로 분리(사용자 확정). `ai_coach`는 향후 로드맵 코치용 스캐폴딩으로 보존.
- **어디** — [consult_session.py](../models/bases/consult_session.py)·[consult_message.py](../models/bases/consult_message.py)·[consult_session_repository.py](../hub/repositories/consult_session_repository.py)·[consult_context_repository.py](../hub/repositories/consult_context_repository.py)(로드맵 쿼리 제거)·[consult_context.py](../hub/services/consult_context.py)·[consult_service.py](../hub/services/consult_service.py)·[self_model_extraction_service.py](../hub/services/self_model_extraction_service.py) · [core/llm/client.py](../../../core/llm/client.py)(`_CONSULT_SYSTEM_PROMPT`) · [consult_routor.py](../../../api/v1/consult/consult_routor.py)·[main.py](../../../main.py)·[scheduler.py](../../../core/scheduler.py)·[env.py](../../../alembic/env.py) · 마이그 `59a2f51cc892` · 프론트 `components/features/consult/*`·`lib/api/consult.ts`·`app/(main)/consult|coach/page.tsx`·`app/chat/page.tsx`. 스펙 [design](../../../docs/superpowers/specs/2026-07-03-consult-room-migration-design.md)·플랜 [plan](../../../docs/superpowers/plans/2026-07-03-consult-room-migration.md).
- **검증** — SDD 3태스크 각 리뷰 Approved(Task1은 source 컬럼 DEFAULT까지 개명하는 Important 수정 라운드 — 마이그 `downgrade→upgrade` 대칭 재적용). whole-branch(opus) Ready:Yes·Critical/Important 0. **Codex 2라운드**: P2 1건(`/chat`이 플레이스홀더 `/coach`로 dead-end)·opus N1(ConsultView 로드맵 첫인상) 수정 후 재검토 클린. 백엔드 관련 11스위트(consult_context13·consult_service13·consult_session_repo11·models_import9·consult_endpoint11·self_model_extraction13·extract_job2·merge10·repo9·scheduler5·recommend_explain6) green + tsc 0. 브리프 밖 발견 정당 수정 3건(self_model_repository의 `coach_session_ref` 참조·`coach_endpoint_test`·`consult_stream_test` 로드맵 단정). 커밋 2ed391f..50fdbc9.
- **후속** — SP-4: 자기모델 레이더·키워드를 실데이터(`GET /api/user/self-model`)로 시각화(현재 목업 레이더 제거만). 로드맵 코치 백엔드(`ai_coach` 재활성)·상담→코치 핸드오프. 코스메틱 개명 스윕(`CoachStreamRequest`/`coach_stream` 핸들러명·`self.coach_repo` 속성·`@/data/coachContext` 파일·공유 프롬프트 `_COACH_SUMMARY`/`_SELF_MODEL_EXTRACT` 문구·잔여 `coach_extraction` 테스트 시드/주석·erd.md §6.6 밖 표기).

## 2026-07-01 — SP-1: 자기모델 데이터층(user_self_model + evidence) 신설
- **무엇** — "AI 상담실=개인화 본가"의 토대. 신규 테이블 `user_self_model`(RIASEC·big_five·narrative_summary·axis_confidence·source, 1행/사용자) + `user_self_model_evidence`(dimension·polarity·content·confidence·is_sensitive·content_hash dedup·coach_session_ref·source, append-only). `SelfModelRepository`(upsert·append dedup·민감 SQL 격리 조회), `SelfModelService`(순수 `merge_structured`: user_form 우위·confidence 게이팅 0.40·source 승격 + camelCase 조회 셰이핑), `GET /api/user/self-model`(비민감 기본·401). 민감 근거는 저장하되 기본 조회에서 SQL로 제외하고, dedup 충돌 시 `is_sensitive`를 모노토닉 승격(재분류 누출 방지).
- **왜** — Sync/Chance 개인화 신호가 얇던 병목을, 상담 대화로 성격·가치관·호불호·제약을 쌓아 두껍게 만들 데이터 토대. 추출-레디 — SP-2(대화 추출)가 `upsert_structured`/`append_evidence(source='coach_extraction')`를 그대로 재사용.
- **어디** — [user_self_model.py](../models/bases/user_self_model.py) · [user_self_model_evidence.py](../models/bases/user_self_model_evidence.py) · [self_model_repository.py](../hub/repositories/self_model_repository.py) · [self_model_service.py](../hub/services/self_model_service.py) · [user_routor.py](../../../api/v1/user/user_routor.py)(`GET /self-model`) · 마이그레이션 `b4cb00cc6a07`(Neon 적용) · [env.py](../../../alembic/env.py) 모델 등록. 스펙 [design](../../../docs/superpowers/specs/2026-07-01-ai-coach-self-model-design.md)·플랜 [plan](../../../docs/superpowers/plans/2026-07-01-self-model-phase1-data-layer.md).
- **검증** — SDD 4태스크 각 spec+품질 리뷰 통과 + opus whole-branch(Ready:Yes, Critical/Important 0). Codex: P1(민감 재분류 누출 — 동일 content 비민감→민감 재삽입 시 기존 비민감 행 잔존) 수정(모노토닉 승격 `is_sensitive = 기존 OR 신규` + `xmax=0` 실insert 카운트, 회귀 3건). P2(polarity dedup)는 판단 후 반영 안 함(테스트 9/9로 실패주장 반증 + like/dislike 구분 위해 polarity 해시 포함이 정상). 테스트 import 8·merge 10·repository 9·endpoint 6 = 33/33(Neon 실DB). 커밋 a93d89b..8f993f0.
- **후속** — ① SP-3 read 경로용 복합 인덱스 `(user_id,dimension)`·`(user_id,is_sensitive)` 미생성(접근 패턴 확정 후 추가). ② `write_self_model` dumb overwrite — 신규 caller는 반드시 full merge 상태 전달. ③ **무관 기존 model↔DB drift**(sectors·sub_sectors·raw_tech_adoption_data·sector_source_map)가 autogenerate에 잡힘 — 이번 마이그에선 수기 제외, 별도 정합 필요(향후 autogenerate 오염 주의). ④ polarity 정규화는 SP-2 추출기 책임. ⑤ 민감정보 암호화·삭제권은 후속 프라이버시 정책.

## 2026-06-30 — 개인화 Phase 3: 프론트 선택 입력 섹션·완성도 미터·온보딩 (프론트 전용)
- **무엇** — Phase 1/2 백엔드 API(`/api/user/profile`·`/api/preferences`·확장 `/api/persona`·`/api/user/sync-profile`)에 프론트를 배선. 프로필 페이지에 자기완결 선택 섹션(기본정보·성향·스펙·관심) + 완성도 미터, 로그인 후 1회 `/onboarding`(건너뛰기), 관심키워드 풀 12섹터+직무군 재설계. 백엔드 무변경.
- **왜** — 사용자가 부담 없이(전부 선택) 성향·스펙을 입력해 Sync/Chance 개인화 품질을 끌어올리는 입력 경로 완성. 가입 직후 토큰 없는 실제 auth 플로우 반영해 온보딩은 로그인 후 트리거.
- **어디** — 프론트 `www.yeotaeho.kr/src/`: `lib/api/{profile,preferences}.ts`·`hooks/{useProfile,usePreferences}.ts`·`data/personalizationOptions.ts`·`components/features/profile/{ChipSelect,BasicInfoSection,PreferencesSection,InterestSection,CompletionMeter}.tsx`+`PersonaForm.tsx`(스펙 4필드)·`app/(main)/profile/page.tsx`·`app/onboarding/page.tsx`·`lib/onboarding.ts`·3 OAuth 콜백. (user_intelligence 도메인 데이터를 서빙하는 프론트라 여기 기록.)
- **검증** — `pnpm exec tsc --noEmit` 0 에러(프론트 unit test 없음). 최종 리뷰(opus): 전체교체 데이터보존·camelCase 계약 백엔드 대비 end-to-end 검증, Critical/Important 0. 커밋 06a4c40..7b46bbf.
- **후속** — useSyncProfile 공유훅 추출 · onboarding 인증가드 early-return · signup 페이지 기존 8 뉴스카테고리→신규 풀 통일.

## 2026-06-30 — 성향·선호(user_preferences) 신설 + persona 스펙 4필드 확장 — 개인화 Phase 1
- **무엇** — (1) 성향·선호(disposition) 수직 신설: `user_preferences`(작업성향·선호 기업규모·근무형태·일의 가치) + `GET/PUT /api/preferences`. (2) 기존 `user_personas`에 스펙 심화 4 JSONB 컬럼(자격증·어학·링크·프로젝트) 추가 + `/api/persona` 확장. 전부 nullable·선택 입력, `source` provenance(미래 coach 추출 재사용 대비).
- **왜** — 개인화 병목(target_job+keywords만 사용) 해소. 성향·스펙은 의미 데이터라 Phase 2에서 임베딩 직렬화 대상 — user_intelligence 도메인에 배치해 사전 분리.
- **어디** — 신규 ORM [user_preference.py](../models/bases/user_preference.py)(테이블 `user_preferences`) · [preference_repository.py](../hub/repositories/preference_repository.py)(work_values JSONB CAST) · [preference_service.py](../hub/services/preference_service.py) · 신규 라우터 [preferences_routor.py](../../../api/v1/preferences/preferences_routor.py)(`/api/preferences`, main.py 등록). persona 확장: [persona_repository.py](../hub/repositories/persona_repository.py)·[persona_service.py](../hub/services/persona_service.py)·[persona_routor.py](../../../api/v1/persona/persona_routor.py)·[user_persona.py](../models/bases/user_persona.py). 마이그레이션 `a3f7c1e9d2b4`(Neon 적용).
- **검증** — `scripts/preferences_endpoint_test.py` 8/8 · `scripts/persona_endpoint_test.py` 13/13(기존 9+신규 4) PASS(Neon 실DB). 커밋 465829d·f0e1ad3·594fb00.
- **후속** — Phase 2: `_user_text()`에 성향(한국어 라벨)·스펙(skill/cert/project) 직렬화 + embed_repository JOIN + 재임베딩 보장 + Chance 키워드 가산. 대화 추출(ai_coach) 파이프라인은 추출-레디 설계만 완료, 구현은 별도 스펙.

## 2026-06-28 — PersonaForm 저장 시 로드맵 자동 재생성 연계
- **무엇** — 페르소나 저장 성공 후 `/api/roadmap/refine` 자동 호출 → 역량 입력 즉시 로드맵에 반영. 재생성 실패는 저장 성공을 막지 않음(try/catch).
- **왜** — 페르소나 수집과 로드맵 생성 사이 수동 단계 제거(역량 입력→개인화 로드맵 즉시 루프).
- **어디** — `www.yeotaeho.kr/src/components/features/profile/PersonaForm.tsx`(useRefreshRoadmap 연계, 버튼 "로드맵 생성 중…").
- **검증** — tsc --noEmit 0 에러. 커밋 7fcc486.

## 2026-06-28 — 구조화 폼 페르소나 수집(도메인 첫 구현)
- **무엇** — 빈 스텁이던 user_intelligence 도메인에 페르소나(스킬·경험·학력·요약) 구조화 폼 수집 기능 구현. Roadmap·Sync 분석의 실데이터 기반.
- **왜** — Roadmap을 실데이터 기반으로 전환하려면 사용자 역량 데이터 수집 주체가 필요. 대화형 LLM 추출 대신 결정론 폼으로 확정(LLM은 RoadmapPlanner에서).
- **어디** — [persona_repository.py](../hub/repositories/persona_repository.py)·[persona_service.py](../hub/services/persona_service.py)(source=user_form) + 라우터 [persona_routor.py](../../../api/v1/persona/persona_routor.py)(`GET/PUT /api/persona`, auth, Pydantic 검증). ORM은 [user_persona.py](../models/bases/user_persona.py)(테이블 `user_personas`, 마이그레이션 c4e7a9d2f6b1). 프론트 `www.yeotaeho.kr/src/lib/api/persona.ts`·`hooks/usePersona.ts`·`components/features/profile/PersonaForm.tsx`(프로필 페이지 마운트).
- **검증** — `scripts/persona_endpoint_test.py` 9/9 PASS(Neon 실DB), tsc --noEmit 0 에러. 커밋 11e7c8c.
- **후속** — PersonaForm 저장 후 자동 로드맵 재생성(/refine) 연계. 대화형 추출(ai_coach SSE)은 향후 별도 레이어.
